import React, { Fragment } from "react";
import Header from "../components/Header/Header";

const index = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default index;
